/* ============================================================
   Sistema de Agendamento UBS — Santa Maria DF
   js/index.js  (Página: Início)
   ============================================================ */

/* DESCRIÇÃO: Este arquivo não contém lógica JavaScript complexa porque 
   a página inicial (index.html) funciona basicamente com navegação estática.
   
   Todos os botões e links da página de início usam onclick="window.location.href='..'"
   para navegar entre as páginas. A tradução e acessibilidade são carregadas 
   automaticamente através dos scripts globais (translations.js e accessibility.js)
   que são incluídos no HTML.
   
   Portanto, este arquivo está vazio ou apenas com comentários.
   A navegação é feita via links HTML (window.location.href). */